NODE_ENV=production
PORT=3014

SMTP_HOST=mail.fulcrum.com.na
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=build@fulcrum.com.na
SMTP_PASS=your-mailbox-password
SMTP_FROM=build@fulcrum.com.na
SMTP_TO=build@fulcrum.com.na

# If deployed on the same machine as the mail server, try:
# SMTP_HOST=127.0.0.1
# SMTP_PORT=587
# SMTP_SECURE=false
